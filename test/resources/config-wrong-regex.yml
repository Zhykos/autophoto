autophoto:
  scan:
    - directory: ./test/resources/video-game
      type: video-game
      data-pattern:
        regex: '^(.+) \(\d{5}\)/(.+)/.+\.webp$'
        groups:
          - title
          - release-year
          - platform
