autophoto:
  scan:
    - directory: ./test/resources/video-game
      type: foo
      data-pattern:
        regex: '^(.+) \(\d{4}\)/(.+)/.+\.webp$'
        groups:
          - title
          - release-year
          - platform
